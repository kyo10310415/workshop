{
  "name": "workshop-management-system",
  "version": "1.0.0",
  "description": "Workshop Management System with PDF materials",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "build": "cd server && npm install && npx prisma generate && npm run build && cd ../client && npm install && npm run build",
    "start": "cd server && npx prisma migrate deploy && node prisma/seed.js && npm start",
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev",
    "postinstall": "cd server && npm install && cd ../client && npm install"
  },
  "keywords": ["workshop", "pdf", "learning", "education"],
  "author": "",
  "license": "MIT"
}
